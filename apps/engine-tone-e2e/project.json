{
  "name": "engine-tone-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "implicitDependencies": ["engine-tone"],
  "targets": {
    "e2e": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{e2eProjectRoot}"],
      "options": {
        "jestConfig": "apps/engine-tone-e2e/jest.config.ts",
        "passWithNoTests": true
      },
      "dependsOn": ["engine-tone:build", "engine-tone:serve"]
    }
  }
}
